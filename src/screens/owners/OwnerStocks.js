import {
  SafeAreaView,
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
} from 'react-native';
import React from 'react';
import {BackButtonIcon, SearchIconVar} from '../../components/Icons';

export default function OwnerStocks() {
  return <Text>OwnerStocks</Text>;
}

const styles = StyleSheet.create({});
